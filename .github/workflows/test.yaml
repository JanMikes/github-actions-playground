name: Test

on:
  - push

jobs:
  output-testing:
    runs-on: ubuntu-latest
    steps:
      -
        uses: actions/checkout@v2

      -
        uses: shivammathur/setup-php@v1
        with:
          php-version: 7.3
          coverage: none

      - run: echo $MY_ENV

      - run: |
        php set-env.php
        echo $MY_ENV
